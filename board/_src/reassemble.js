// No-op. The board source is committed directly to the repo now,
// so nothing needs reassembling. Present only so a leftover Vercel
// install-command override ("node _src/reassemble.js && npm install") still succeeds.
process.exit(0);
